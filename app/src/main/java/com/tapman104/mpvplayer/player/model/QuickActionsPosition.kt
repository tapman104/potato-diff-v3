package com.tapman104.mpvplayer.player.model

/**
 * Controls where the quick-action buttons (audio, subtitle, decode-mode) are positioned
 * in the player overlay.
 */
enum class QuickActionsPosition {
    TOP_LEFT,
    TOP_RIGHT,
    BOTTOM_LEFT,
}
